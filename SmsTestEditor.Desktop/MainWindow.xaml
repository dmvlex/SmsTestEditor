<Window x:Class="SmsTestEditor.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmsTestEditor.Desktop"
        xmlns:converters="clr-namespace:SmsTestEditor.Desktop.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.InputBindings>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveAllValuesCommand}"/>
    </Window.InputBindings>
    <Window.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>
    <Grid>
        <!--a.kh: При сохранении значений блокирую DataGrid.-->
        <DataGrid ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyleRounded}" ItemsSource="{Binding Variables}"
                  CanUserAddRows="False"
                  IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBoolConverter}}"
                  AutoGenerateColumns="False">
            
            <DataGrid.Resources>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsVariableValueChanged}" Value="True">
                            <Setter Property="Background" Value="LightYellow"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Поле" 
                                    Binding="{Binding Name}" 
                                    IsReadOnly="True"
                                    Width="*"/>
                <DataGridTextColumn Header="Значение" 
                                    Binding="{Binding Value}" 
                                    Width="*"/>
                <DataGridTextColumn Header="Комментарий" 
                                    Binding="{Binding Comment}"
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
