<Window x:Class="SmsTestEditor.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmsTestEditor.Desktop"
        xmlns:converters="clr-namespace:SmsTestEditor.Desktop.Converters"
        mc:Ignorable="d" 
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True" WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="450" Width="800">
    
    <Window.InputBindings>
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{Binding SaveAllValuesCommand}"/>
    </Window.InputBindings>
    <Window.Resources>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>
    <Grid>
        <Border CornerRadius="20" Background="White" BorderBrush="Transparent" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid x:Name="TitleBar" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                      Grid.Row="0" Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Margin="15,0,0,0"
                               VerticalAlignment="Center"
                               Text="Тестовое WPF-приложение для SmartMealService"/>
                    <StackPanel Grid.Column="1" Margin="0,0,15,0"
                                Background="Transparent"
                                HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button x:Name="MinimizeButton" Style="{DynamicResource WindowControlButton}"
                                Content="&#xE921;" Click="MinimizeButton_Click"/>
                        <Button x:Name="CloseButton" Style="{DynamicResource WindowControlButton}"
                                Content="&#xE8BB;" Click="CloseButton_Click"/>
                    </StackPanel>
                    
                </Grid>
                <DockPanel Margin="5" Grid.Row="1">
                    <!--a.kh: При сохранении значений блокирую DataGrid.-->
                    <DataGrid Style="{DynamicResource DataGridStyleRounded}" 
                              ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyleRounded}" 
                              ItemsSource="{Binding Variables}"
                              CanUserAddRows="False" HeadersVisibility="Column"
                              IsEnabled="{Binding IsSaving, Converter={StaticResource InverseBoolConverter}}"
                              AutoGenerateColumns="False">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsVariableValueChanged}" Value="True">
                                        <Setter Property="Background" Value="LightYellow"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Поле" 
                                                Binding="{Binding Name}" 
                                                IsReadOnly="True"
                                                Width="*"/>
                            <DataGridTextColumn Header="Значение" 
                                                Binding="{Binding Value}" 
                                                Width="*"/>
                            <DataGridTextColumn Header="Комментарий" 
                                                Binding="{Binding Comment}"
                                                Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
